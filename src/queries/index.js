const QUERY_PULL_REQUEST = require('./pullRequests');
const QUERY_COMMIT_COMMENTS = require('./commitComments');
const QUERY_ISSUES = require('./issues');
const QUERY_USERS = require('./users');
const QUERY_COMMITS = require('./commits');
const QUERY_REPOSITORY = require('./repository');

module.exports = {
  QUERY_PULL_REQUEST,
  QUERY_COMMIT_COMMENTS,
  QUERY_ISSUES,
  QUERY_USERS,
  QUERY_COMMITS,
  QUERY_REPOSITORY,
};
